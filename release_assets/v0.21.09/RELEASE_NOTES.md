# Release Notes

## v0.21.09

- Reduced the remaining oversized Wi-Fi password fields in both the discovered-network connect popup and the AP password row.
- Standardized compact password and PIN input sizing across popups and menu screens so focused and reopened fields keep the smaller layout.
- Reapplied compact textarea geometry after password visibility toggles and modal reopen paths to prevent LVGL state changes from restoring larger metrics.
- Updated release documentation and packaged binaries for the new firmware version.